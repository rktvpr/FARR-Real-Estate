import React from 'react';

const Header = () => {
    // const logout = (event) => {
    //     event.preventDefault();
    //     Auth.logout();
    // };
    return (
        <header className="">
            test
        </header>
    )

}

export default Header